---
type: Backlog Item
title: 仕様を0.2版へ改訂する
description: 設計詰めで決まった内容を HARNESS_SPEC.md に反映し、第0.2版とする。
tags: [shared]
status: stable
layer: shared
generated:
  by: "process:okf-cli"
  at: "2026-08-31T15:13:56Z"
state: doing
priority: high
effort: M
feasibility: B
ai: assisted
cost: false
created: "2026-09-01"
done_at: null
accepts: []
spec: ["§1.1", "§2", "§3", "§4", "§5.2", "§7", "§8.1", "§11", "§12"]
target: harness
evidence: []
related: ["/backlog/T-0002-pre-start-checklist.md"]
---

# 仕様を0.2版へ改訂する

## やりたいこと

2026-09-01 の設計詰めで決まった内容を `HARNESS_SPEC.md` に反映し、第0.2版とする。段階1の定義そのものが変わるため、追補ではなく版を上げる。

## 背景・現状

第0.1版は「段階1 = ファイル9枚を新規に置く」という前提で書かれている。この前提は次の3点で現実と合わなくなった。

1. **対象プロジェクトが確定した。** 段階1の対象は `okf-devkit`（ルートは `LICENSE` / `README.md` / `pyproject.toml` / `src` / `tests` のみ。入口ファイルも docs バンドルも無い）。CI は ubuntu/windows × Python 3.11/3.13 のマトリクスで `tests/run_all.py` と e2e smoke を実行しており、REQ-1 の強制点を初日から CI に置ける。
2. **段階3の配布先が具体化した。** `ScheLiveApp` には既に OKF v0.2 バンドル・5ジョブの CI・289行の `CLAUDE.md`・2製品分の Stop フックがある（2026-09-01 調査）。仕様が想定する役割の大半を**別の形で**満たしているため、第4節の構成図を「既定の配置」から「役割 → 実配置の写像表」に格上げしないと、配布時に「並置して二重運用」しか選べなくなる。
3. **誤記が2点残っている。** 第1節の「コア5枚」は第4節のツリーでは4枚（実質8ファイル）。第3節冒頭の「16項目」は採番が①〜⑳の20項目。バックログの `accepts` がこの採番を参照するため、先に固定する必要がある。

## 進め方

`HARNESS_SPEC.md` に次の14点を反映する。

| # | 箇所 | 変更内容 |
|---|---|---|
| 1 | ヘッダ | 版を 0.2、日付を 2026-09-01 に |
| 2 | 冒頭の注記 | 「ファイル9枚」→「入口2+コア4+プロジェクト2=8ファイル、加えて `.claude/settings.json` への追記1件」 |
| 3 | §1.1 | 決定の振り分け3分岐（SPEC / タスクファイル / 台帳）と「末尾に行き先を書く」を編集規則に追加。`BACKLOG.md（未作成）` の行を `docs/backlog/`（OKF バンドル）に更新 |
| 4 | §2 | 対象プロジェクト=`okf-devkit`、最初の製品=Claude Code、対象OS=Windows 11 + Python 3.14、必須検証=`pip install -e .` / `python tests/run_all.py` を記入。残り3欄は T-0002 |
| 5 | §2 | 「ハーネス自身は対象にしない」に注記を追加——**harness リポジトリには R8・第11節が効かない** |
| 6 | §3 段階1 | 「9ファイルを作る」→「役割の充足を確認し、欠けている役割を埋める」に定義変更 |
| 7 | §3 段階1 | 冒頭の「16項目」→「DESIGN 第16章の16項目を段階配分にあたり20項目へ分割・具体化した。採番①〜⑳を正とする」 |
| 8 | §3 段階3 | 配布先を `ScheLiveApp` と明記（既存の仕組みとの衝突が段階3の試験になる） |
| 9 | §4 | 構成図の前に「役割 → 実配置の写像表」を追加し、構成図はその**既定値**と位置づける |
| 10 | §5.2 / §7 | 必須検証表に「対象層」列を追加。触った層 + docs は必須、他は「未実行」と明記する規則 |
| 11 | §7 | REQ-1 の強制点を「運用◇+独立再検証」→「CI（対象に CI がある場合）」へ昇格 |
| 12 | §8.1 | ツールが生成するネストした入口（`okf init` の `docs/AGENTS.md` 等）の扱いを1行追加 |
| 13 | §11 | 「作らないもの」は**新規作成の抑制**であり、対象プロジェクトに既にある自動化の撤去を求めるものではない、を1行追加 |
| 14 | §12 / 末尾 / 改訂履歴 | 「CI の導入・強制点の昇格」を保留から外す。末尾の「0.2版へ改訂する」→「0.3版」。改訂履歴に 0.2 の行を追加 |

反映後、`HARNESS_SPEC.html` を再生成し、**同じ Artifact URL**（`7da5b498-d9bf-4d55-8027-61011b829ee6`）へ再公開する。

## 決定と根拠

- 段階1の対象は `okf-devkit`、`ScheLiveApp` は段階3の配布先に回す → SPEC §2 / §3段階3
- 段階1の定義を「9ファイルを置く」から「役割の充足を確認し欠けを埋める」へ → SPEC §3段階1 / §4
- 必須検証は層別、強制点は最初から CI → SPEC §5.2 / §7
- 「作らないもの」は新規作成の抑制であって既存自動化の撤去要求ではない → SPEC §11
- ツールが生成するネスト入口は R8 の対象外 → SPEC §8.1
- harness リポジトリ自身はハーネスの適用対象ではないので R8・§11 が効かない → SPEC §2
- 決定の振り分け3分岐と「末尾に行き先を書く」→ SPEC §1.1
- 版は本タスク完了で 0.2、段階1完了で 0.3。HTML 再生成はこの2回のみ → SPEC §1.1 / 末尾
- バックログは harness リポジトリを OKF バンドル化して `docs/backlog/` に置く。`type: Backlog Item` / `prefix: T` / 独自キー `accepts`・`spec`・`target`・`evidence` → 本タスクで実施済み（`okf.yml` / `docs/CONVENTIONS.md`）
- `okf-devkit` は `pip install -e ../okf-devkit` で入れ、harness の日々の運用をドッグフーディングにする → T-0002

**台帳（K-）行きの観測**（台帳は T-0004 で作るので、それまでここに置く）:

- ローカルの Python は 3.14.3 だが、`okf-devkit` の CI マトリクスは 3.11 / 3.13、classifiers も 3.11〜3.13 まで。**実際に使っている版がテストされていない** → K- 候補（分類: 環境差）
- `pip install -e` で入る `okf.exe` が PATH に無く、`python -m okf_devkit.cli` でしか呼べない → K- 候補（分類: 環境差）
- `okf new backlog` は `_templates/backlog-item.md` の `tags:` を無視し、`--layer` の値で上書きする → K- 候補（分類: 能力補助）
- `okf index --write` が生成する `backlog/index.md` は、同じ `state` の中が ID 順に並ばない（読む順序が不定になる） → K- 候補（分類: 能力補助）

## 完了条件

- [x] `HARNESS_SPEC.md` の14点がすべて反映されている
- [x] 改訂履歴に 0.2 版の行がある
- [ ] `HARNESS_SPEC.html` を再生成し、同一 Artifact URL へ再公開した
- [x] バックログの `accepts` が参照する採番①〜⑳が SPEC.md 側と一致している
- [ ] 必須検証を実行し、下の検証記録を4値で埋めた
- [ ] `evidence` に成果物側のコミット SHA を記入した
- [ ] 本ファイルの `state` を `done` にし `done_at` を記入した

## 検証記録

| 識別子 | 結果 | 対象（コミット・版） | 証拠（要約・ログ所在） |
|---|---|---|---|
| spec-consistency | 成功 | HARNESS_SPEC.md 第0.2版 / HARNESS_SPEC.html | 14点の反映をそれぞれ grep で確認。HTML はタグ収支一致・ブラウザ描画確認済み |
| artifact-publish | 未実行 | — | 再公開はユーザーの承認待ち |

※結果は **成功 / 失敗 / 未実行 / 実行不能** のいずれか。失敗・未実行を成功と書かない。

## 結果

<完了時に記入する。実際に変更したファイル、想定と違った点、台帳に回した観測。>
