# 共通ハーネス構築仕様

整理番号 HSP-2026-0830/第0.2版
HARNESS_DESIGN.md の設計方針と審査記録(HRV-2026-0830)の指摘を反映した、実際に構築するハーネスの実装仕様。

| 項目       | 内容                                                                                                                         |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 依拠文書   | HARNESS_DESIGN.md(設計方針)/ HARNESS_REVIEW.html(審査記録。指摘 R1〜R10・仮説 H1〜H6・確認事実 V1〜V12 を本仕様から参照する) |
| 対象製品   | Claude Code(段階1)→ Codex CLI・GitHub Copilot(段階2)。対象OSは Windows を含み、特権・シンボリックリンクを前提にしない        |
| 記載の状態 | 【確定】=本仕様で固定 【着手時に記入】=実証開始時に第2節で確定 【保留】=決めずに実証へ進んでよい                             |

> **実証用 第0.2版** — 段階1でやるのは、対象プロジェクトが**仕様の求める役割を満たしているかの確認と、欠けている役割の補完**である。何もない状態から始める場合の既定は**8ファイル**(入口2枚+コア4枚+プロジェクト2枚)と、`.claude/settings.json` への追記1件であり、ツール・スクリプト・フックは作らない。この仕様の第11節「作らないもの」が、第1節の作るものと同格の規定である。

## 1. 位置づけと設計原則

本仕様は HARNESS_DESIGN.md を置き換えるものではない。設計方針書は「なぜそうするか」の根拠集として凍結し、本仕様が「いま何を作るか」を定める。実装時に迷ったら、本仕様→審査記録→設計方針書の順に参照する。

### 1.1 文書の役割と編集規則【確定】

| 文書                       | 役割                   | 扱い                                                                                                       |
| -------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------- |
| HARNESS_DESIGN.md          | 「なぜ」の根拠集       | 凍結。以後編集しない                                                                                       |
| HARNESS_REVIEW.html        | 審査の記録             | 凍結。完了した時点記録                                                                                     |
| **HARNESS_SPEC.md(本書)**  | 「何を作るか」の正本   | 生きた文書。変更はここにだけ反映する                                                                       |
| HARNESS_SPEC.html          | 閲覧用スナップショット | 手編集禁止。版が上がる節目に本書から再生成し、同じ Artifact URL へ再公開する                               |
| `docs/backlog/T-NNNN-*.md` | 作業単位と順序         | 本書第3節から起こした。1タスク1ファイル。ハーネス構築用リポジトリを OKF バンドルとし、`okf` CLI で管理する |

同じ内容を二箇所に書かない。仕様書内のテンプレート(第5節)は、段階1で対象プロジェクトに実体化した時点でそちらを正本とし、本書は参照に切り替える。

**決定の振り分け【確定】** — 作業中に決まったことは、次の3つのうち**1箇所にだけ**書き、他所からは参照する。書き留めるときは末尾に行き先を記す(`→ SPEC §x.y` / `→ T-000N` / `→ K-00N`)。

| 決めたこと                                            | 行き先                     | 見分け方                               |
| ----------------------------------------------------- | -------------------------- | -------------------------------------- |
| 他のタスクでも、別のプロジェクトでも成り立つ規定      | 本書                       | 固有名詞が要らない                     |
| そのタスクを終わらせるためだけの情報                  | バックログのタスクファイル | プロジェクト名・パス・既存の事情が入る |
| 仕様の欠陥かもしれないが、まだ1回しか起きていない観測 | 改善台帳(第5.5節)          | 変えたいが根拠が1件しかない            |

### 1.2 審査記録の反映

- **R1**: 受け入れ条件を第3節で段階ごとに割り当てた。段階1の合格基準は8項目に絞られる。
- **R2**: 補助機構の「作らない既定」を第11節に、再導入トリガーとともに明文化した。
- **R3**: 必須制約に「強制点」の欄を設け(第7節)、強制点が運用のみの項目は運用ルールへ降格表示する。
- **R4**: 配置と所有区分を第4節で確定した(ディレクトリ境界=所有区分)。
- **R5**: 宣言付き上書きを第9節で定義した。
- **R6**: 完了条件の保護水準を第7節で明示した(deny ルール+独立再検証。CI があれば強制点に昇格)。
- **R7**: 入口への自動追記は持たない。接続行は人が追加する(第9節)。
- **R8**: 入口はルートの AGENTS.md 1枚のみとする(第8節)。
- **R9**: 記録の上限に既定値を置いた(第5節・第6節)。
- **R10**: 報告・引継ぎ・作業記録を1テンプレートに統合した(第5節)。

### 1.3 三つの原則【確定】

1. **ファイルだけで始める。** 段階1〜2の実体はプレーンな Markdown とディレクトリ構成のみ。ツールは、実測された摩擦を根拠にしてからしか作らない。
2. **指示は文脈、強制は仕組み。** 守らせたいことは製品設定・実行環境・CI のいずれかに置き、置けないものを「必須」と呼ばない(V10: CLAUDE.md は公式に「強制ではなく文脈」)。
3. **足すときに、外し方を書く。** 手順・スキル・ルールを1つ足すたびに、台帳へ分類(能力補助/方針/環境差/安全)と見直し契機を1行書く。書けないものは足さない。

## 2. 着手前チェックリスト

段階1の開始時にここを埋める。埋めるまで実装を始めない(R4)。推奨値は審査記録第5節による。

**本仕様・バックログを置くリポジトリ自身は、ハーネスの適用対象ではない【確定】。** したがって第8.1節(R8: 入口は1枚)と第11節(作らないもの)は、そのリポジトリには適用しない——ツールが生成する入口やフックの雛形をそのまま使ってよい。適用対象は下表の「対象プロジェクト」だけである。

| 項目                    | 状態             | 記入欄・推奨                                                                                                    |
| ----------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------- |
| 対象プロジェクト        | 【確定】         | `okf-devkit`(段階1)。段階3の配布先は `ScheLiveApp`。ハーネス自身は対象にしない                                  |
| 改善したい摩擦・上位3件 | 【着手時に記入】 | 1. ＿＿ / 2. ＿＿ / 3. ＿＿(実際に起きた手戻りから選ぶ。台帳の最初の3行になる)                                  |
| 最初の製品              | 【確定】         | Claude Code(現在の主環境。強制点が最も揃う)                                                                     |
| 対象OS・ランタイム      | 【確定】         | Windows 11 / Python 3.14.3(ローカル)。CI は ubuntu・windows × Python 3.11・3.13。特権・シンボリックリンク不使用 |
| 必須検証のコマンド      | 【確定】         | ビルド `python -m pip install -e .` / テスト `python tests/run_all.py` / 静的解析 なし(未導入。新設しない)      |
| 代表作業(評価用)3件     | 【着手時に記入】 | 小さな不具合修正 ＿＿ / 既存構成に沿う機能追加 ＿＿ / セッションをまたぐ再開 ＿＿                               |
| 許容できない悪化        | 【着手時に記入】 | 例: 小修正の所要が現状の1.5倍を超える/必須検証の虚偽報告が1件でも出る                                           |
| 初期の共通手順          | 【確定】         | 「検証と報告」「中断と引継ぎ」の2本のみ。計画手順は大きな作業が実際に来たときに追加                             |

## 3. 段階計画と受け入れ条件

設計方針書第15.2節の順序に、第16章の受け入れ条件16項目を割り当てて統合した(R1)。**段階配分にあたり16項目を20項目へ分割・具体化しており、以下の採番①〜⑳を正とする**(バックログの `accepts` はこの番号を参照する)。各段階は「出口の確認」を満たすまで次へ進まない。一つの製品で動いただけでは複数製品対応と呼ばない。

### 段階1 単一プロジェクトでの実証(Claude Code/目安2〜4週の実作業)

- **作るもの**: 第4節の写像表で**欠けている役割**にあたるファイル。何もない状態から始めるなら8ファイルと `.claude/settings.json` への追記1件。スキル・フック・スクリプトは作らない。
- **やること**: 実際の作業に統合テンプレートと2手順を使い、台帳へ摩擦を記録する。コア昇格級の変更はまだ発生させない。
- **受け入れ条件**:
  - ① 入口から必要な手順へ到達できる
  - ② 個人のグローバル設定(`~/.claude` 等)に必須要件が隠れていない
  - ③ 特権なしで導入できている
  - ④ 検証の失敗・未実行が失敗・未実行のまま報告される
  - ⑤ 引継ぎからの再開が1回成功する(H5)
  - ⑥ 現状比較の記録が残る(第10節の軽量評価)
  - ⑦ 手順が不要な小作業に重い工程を強いていない
  - ⑧ 手順を外した試行(部品なし比較)が可能である
- **出口の確認**: 受け入れ①〜⑧と、仮説 H2(カナリア記録率)・H3(転記精度)・H5(再開)の初回計測が済んでいる。

### 段階2 製品間の確認(Codex CLI → Copilot(VS Code エージェントモード))

- **作るもの**: 対応表(第8節の様式)1枚。必要ならスキル化の試行(H1 の検証リポジトリ)。
- **やること**: 同じ入口・同じ手順・同じ作業記録で代表作業を各製品1〜2件実施し、対応表を実測で埋める。H1(スキル重複挙動)を検証リポジトリで確認する。
- **受け入れ条件**:
  - ⑨ 各製品の対象利用形態で、入口から手順に到達し同じ成果物・検証記録を残せる(基本モード成立。カナリアで測る)
  - ⑩ スキルを使う場合、対象外の作業で誤発動しない
  - ⑪ 同名スキルの重複発見が起きない配置が確認できている(H1)
  - ⑫ 強制手段のない製品・利用形態について、制限事項が対応表に明記されている(R3)
- **出口の確認**: 対応表のすべての行に検証日が入り、「文書上の対応」と「実機で確認した対応」が区別されている。

### 段階3 別プロジェクトへの配布(`ScheLiveApp`)

- **作るもの**: `harness-core` リポジトリ(コアの抽出)、`install.json`(第9節)、導入・更新の手順書。スクリプト化は任意。
- **やること**: コアを版付けして抽出し、`ScheLiveApp` へ版固定で導入する。更新・巻戻し・撤去を1回ずつ演習する。**配布先には既に OKF バンドル・CI・独自の入口構成・フックがあり、第4節の写像表と衝突処理がそのまま試験になる。**
- **受け入れ条件**:
  - ⑬ どの版とどの部品を導入しているか確認できる
  - ⑭ 更新前に変更・削除・競合が一覧で分かる
  - ⑮ 更新は1コミットで適用され、途中失敗が残らない
  - ⑯ プロジェクト固有の設定・利用者の編集が更新で失われない
  - ⑰ 同じ導入を再実行しても重複が増えない
  - ⑱ 共通化した改善の取り込み後、不要になったローカルの重複・暫定修正を外せる
  - ⑲ ロールバック・撤去の後もプロジェクト本来の開発・検証ができる
- **出口の確認**: 更新・巻戻し・撤去の演習記録が作業記録として残っている。

### 段階4 自動化の追加(実測された摩擦があるものだけ)

- **作るもの**: 実測で有効だった手順のフック化・検査スクリプト・導入補助など、第11節のトリガーを満たしたものだけ。
- **受け入れ条件**: ⑳ 自動化した処理に上限(範囲・回数・時間)と停止条件があり、必須処理の失敗が隠れない(設計方針書第14.3節)。自動化自体の保守負担が、置き換えた手作業より小さいことを台帳で確認する。

## 4. リポジトリ構成(段階1)【確定】

ディレクトリ境界そのものを所有区分の表示とする(R4)。マーカー・追跡台帳・生成物は持たない。

**まず役割の充足を確認する【確定】。** 導入先が既に別の形で同じ役割を満たしていることがある。その場合は同じ役割の器を二重に作らず、**下の写像表を1枚書いて実配置を指す**。二重に持てば必ず片方が腐る(第1.1節)。

| 役割               | 何をするものか                          | 既定の配置                                             | 実配置(導入先ごとに記入) |
| ------------------ | --------------------------------------- | ------------------------------------------------------ | ------------------------ |
| 入口               | 短い1枚。ここから必要な参照先へ到達する | `AGENTS.md`                                            |                          |
| 製品接続           | 入口を各製品に読ませる差分              | `CLAUDE.md`                                            |                          |
| 手順(検証と報告)   | 検証結果を4値で記す手続き               | `harness/core/procedures/verify-report.md`             |                          |
| 手順(中断と引継ぎ) | 中断・再開の手続き                      | `harness/core/procedures/handover.md`                  |                          |
| 作業記録           | 報告・引継ぎを兼ねる1作業1ファイル      | `harness/state/journal/` + `core/templates/worklog.md` |                          |
| 必須制約           | 強制点つきの守るべき条件                | `harness/core/policy/requirements.md`                  |                          |
| プロジェクト設定   | コマンド・必須検証・対象範囲・例外      | `harness/project/config.md`                            |                          |
| 改善台帳           | 摩擦の観測→候補→試行の器                | `harness/ledger.md`                                    |                          |
| 強制点             | 製品設定・CI による強制                 | `.claude/settings.json` の deny / 既存 CI              |                          |

役割が欠けている場合の**既定の配置**は次の通り。何もない状態から始めるなら、この形をそのまま作る。

```
<プロジェクト>/
├─ AGENTS.md                    ← 入口の正本(プロジェクト所有・1枚のみ)
├─ CLAUDE.md                    ← @AGENTS.md 取込+Claude固有差分(プロジェクト所有)
└─ harness/
   ├─ core/                     ← 将来の共通化候補。汎用の書き方のみ。段階3で配布側所有になる
   │  ├─ procedures/
   │  │  ├─ verify-report.md    ← 手順: 検証と報告
   │  │  └─ handover.md         ← 手順: 中断と引継ぎ
   │  ├─ templates/
   │  │  └─ worklog.md          ← 作業記録の統合テンプレート
   │  └─ policy/
   │     └─ requirements.md     ← 必須制約(利用者のみ編集。denyで保護)
   ├─ project/                  ← プロジェクト所有。共通更新で触らない領域
   │  ├─ config.md              ← コマンド・対象範囲・必須検証・例外
   │  └─ extensions/            ← 独自手順・独自スキルの置き場(最初は空)
   ├─ state/                    ← 作業記録。配布対象外。コミットして引継ぎに使う
   │  ├─ journal/               ← 1作業1ファイル(YYYY-MM-DD-題名.md)
   │  └─ archive/               ← 完了後90日で移動(R9の既定値)
   └─ ledger.md                 ← 改善台帳(プロジェクト所有)
```

- **core/ に置いてよいのは、別のプロジェクトへそのままコピーしても嘘にならない文章だけ。** プロジェクト名・コマンド・パス・内部事情が混ざるものはすべて `project/` へ。この規律が段階3の抽出コストを決める。
- `state/journal/` はコミットする(マシンをまたぐ引継ぎに使うため)。大きなログ・生成物は `.gitignore` し、記録には所在だけ書く。機密・認証情報はどこにも書かない。
- ファイル数を増やす変更(手順の追加、スキル化)は、台帳の候補として記録してから行う(第1節・原則3)。

## 5. ファイル仕様とテンプレート

以下は雛形であり、そのままコピーして使える。〈 〉の箇所を書き換える。文体・項目名は運用しながら変えてよいが、**検証結果の4値(成功/失敗/未実行/実行不能)と、作業記録の節構成は共通契約として維持する**(これが段階3以降にプロジェクト間で揃っている必要のある部分)。

### 5.1 入口の正本【確定】

`AGENTS.md`(プロジェクト所有/60行以内・Codex の合計32KiB上限に留意(V5)):

```markdown
# 〈プロジェクト名〉 — エージェント向け入口

このリポジトリの入口はこのファイル1枚のみ。サブディレクトリに入口を置かない。
ここにない詳細は、作業に応じて下記の参照先を読む(最初から全部は読まない)。

## プロジェクトの要点

- 概要: 〈1〜2行〉
- コマンド・対象範囲・例外: harness/project/config.md
- 必須制約: harness/core/policy/requirements.md(弱めない。衝突したら作業を止めて報告)

## 作業の進め方(規模で選ぶ)

| 作業                       | 進め方                                                                               |
| -------------------------- | ------------------------------------------------------------------------------------ |
| 小さく影響が限定された変更 | 変更 → 必須検証 → 記録(handover不要。計画書も不要)                                   |
| 通常の機能追加・不具合修正 | 作業記録を作り完了条件を書く → 実装 → 必須検証 → 記録                                |
| 影響が大きい・不確実な変更 | 作業記録に計画(目的・対象外・完了条件・変更範囲)を書き、判断が必要な点を先に報告する |
| セッションをまたぐ作業     | 中断前に harness/core/procedures/handover.md に従う                                  |

## 作業記録(報告・引継ぎを兼ねる)

- 場所: harness/state/journal/YYYY-MM-DD-〈題名〉.md(1作業1ファイル)
- 様式: harness/core/templates/worklog.md をコピーする
- 報告は記録の「報告(要約)」節で行う。別の報告文書を作らない

## 検証

- harness/core/procedures/verify-report.md に従う
- 結果は必ず 成功/失敗/未実行/実行不能 のいずれかで記す。失敗・未実行を成功と書かない
```

`CLAUDE.md`(プロジェクト所有/取込は起動時に展開される(V2)ため、ここにも長文を置かない):

```markdown
@AGENTS.md

## Claude Code 固有

(Claude Code だけに必要な差分が実際に出るまで空にしておく)
```

### 5.2 プロジェクト設定

`harness/project/config.md`(プロジェクト所有):

```markdown
# プロジェクト設定

## コマンド

| 用途     | コマンド              | 作業ディレクトリ | 備考 |
| -------- | --------------------- | ---------------- | ---- |
| ビルド   | 〈例: npm run build〉 | ルート           |      |
| テスト   | 〈例: npm test〉      | ルート           |      |
| 静的解析 | 〈例: npm run lint〉  | ルート           |      |

## 必須検証(この表が完了条件の機械判定部分)

| 識別子 | 対象層 | 確認する条件 | コマンド | 実行上の制約 |
| ------ | ------ | ------------ | -------- | ------------ |
| build  | 全体   | ビルドが通る | 上記     | 外部通信なし |
| test   | 全体   | テストが通る | 上記     |              |

※**触った層と docs の行は必須。他の層の行は「未実行」と明記する**——小さな変更に全層の検証を強いないため(第6.1節)。
※ローカルで実行できない行は「実行不能」とし、CI の結果で埋める。
※この表の削除・緩和は通常の実装修正と区別する(requirements.md REQ-2)。

## 対象範囲

- 対象: 〈…〉
- 対象外(このハーネスでは扱わない作業): 〈…〉

## 採用している共通手順

- verify-report / handover

## 承認済みの例外

| 日付 | 内容 | 理由 | 見直し契機 |
| ---- | ---- | ---- | ---------- |
```

### 5.3 作業記録(報告・引継ぎ統合テンプレート)【確定】

設計方針書 7.2/7.3/8.2 の項目を1枚に統合した(R10)。報告=「報告(要約)」節、引継ぎ=状態を「中断」にした記録の全体、と定義する。

`harness/core/templates/worklog.md`(コア(共通化候補)):

```markdown
# 作業記録: 〈題名〉

- 日付: 2026-MM-DD / 製品: 〈Claude Code 等〉 / モデル: 〈確認できた場合のみ実IDを記す。不明なら「不明」〉
- 状態: 進行中 | 中断(引継ぎ可) | 完了

## 目的と完了条件

- 目的:
- 完了条件: 必須検証〈識別子…〉+〈この作業固有の条件〉
- 対象外:

## 範囲と変更箇所

-

## 判断と理由

-(確定した判断・却下した選択肢・完了条件やテストを変更した場合はその前後と理由)

## 検証記録

| 識別子 | 結果 | 対象(コミット・版) | 証拠(要約・ログ所在) |
| ------ | ---- | ------------------ | -------------------- |

※結果は 成功/失敗/未実行/実行不能 のみ。

## 残作業と次の一手

- 残:
- 次:
- 妨げ:
- 前提(ブランチ・環境・未コミットの有無):

## 報告(要約)

- 達成:
- 確認済み:
- 未確認:
- 判断が必要:
```

### 5.4 共通手順(2本)【確定】

末尾の確認符号は仮説 H2(手順が実際に読まれているか)の計測装置であり、段階2の完了後に外してよい。

`harness/core/procedures/verify-report.md`(コア(共通化候補)):

```markdown
# 手順: 検証と報告

1. harness/project/config.md の「必須検証」表のコマンドを順に実行する。
2. 各結果を作業記録の検証記録表へ記す。
   - 失敗: 失敗のまま記す。修正は別の変更として扱い、修正後に再実行する。
   - 未実行: 理由を添えて未実行と記す。
   - 実行不能(環境上の理由): 理由と、代わりに確認できた範囲を記す。
3. 完了条件・テスト自体を変更した場合は、変更前後の期待動作と理由を
   「判断と理由」に書く。同じ変更で失敗を成功に読み替えていないか見直す。
4. 「報告(要約)」の4項目(達成/確認済み/未確認/判断が必要)を埋める。
5. この手順を使ったら、報告末尾に確認符号〔VR〕を記す。
```

`harness/core/procedures/handover.md`(コア(共通化候補)):

```markdown
# 手順: 中断と引継ぎ

中断するとき:

1. 作業記録の状態を「中断(引継ぎ可)」にする。
2. 残作業と次の一手・妨げ・前提を埋める(推測で埋めない。不明は不明と書く)。
3. 検証記録を最新化する(未実行は未実行のまま)。

再開するとき: 4. 記録を読み、現在のファイル・ブランチ・検証結果と照合してから続ける。
古い記録の「完了」だけを根拠に完了扱いにしない。5. この手順で再開したら、記録に確認符号〔HO〕を記す。
```

### 5.5 改善台帳【確定】

設計方針書 10.3(記録項目)・10.4(状態)・10.6(見直し契機)を1ファイルに統合。上限の既定値は R9 による(数字は運用で変えてよいが、既定を消さない)。

`harness/ledger.md`(プロジェクト所有):

```markdown
# 改善台帳

運用規則:

- 状態は 観測 → 候補 → 試行 → 採用 | 却下 | 廃止 の順に進める。
- 有効な行(観測〜試行)は10件まで。超えたら統合するか却下する。
- 同じ症状は新しい行にせず「回数」を加算する。
- 振り返りから直接ルール・手順を書き換えない。必ず台帳を経由する。
- 一度の失敗で恒久ルールを作らない。ただし安全上の欠陥は回数を待たず対処する。
- 採用したら「反映先」を書き、反映先のファイルにも台帳IDを書く(例: K-003)。
- 棚卸し: モデル・製品の変更時は必ず全行を見る(分類「能力補助」は削除候補)。
  それ以外は四半期に一度。
- 分類: 能力補助 | 方針 | 環境差 | 安全

| ID    | 状態 | 初回 | 回数 | 分類 | 症状(1行) | 対処案と適用範囲 | 確認方法 | 結果・反映先 |
| ----- | ---- | ---- | ---- | ---- | --------- | ---------------- | -------- | ------------ |
| K-001 | 観測 |      | 1    |      |           |                  |          |              |
```

## 6. 運用フロー

### 6.1 日々の作業

1. 作業の規模を AGENTS.md の表で選ぶ。**小さな変更に計画書・独立レビュー・新規テストの一律追加を求めない**(設計方針書 7.1・9.3)。
2. 通常以上の作業では `journal/` に記録を1枚作り、完了条件を先に書く。
3. 実装後、「検証と報告」手順を実行する。報告は記録の要約節で完結させる。HTML等の別形式は、説明が複雑なときだけの選択肢とする。
4. 摩擦(繰り返した探索、誤った検証、不要な質問、誤発動)を感じたら台帳へ1行。**その場でルールを増やさない。**
5. 影響の大きい変更では、独立した確認を挟む。現環境では Codex CLI への依頼が「実行製品を差し替えられるレビュー役」(設計方針書 7.4)の最も安価な実装になる。使用した実モデルが確認できない場合は不明と記録する。

### 6.2 変更の二層方針【確定】

| 層   | 対象                                                                             | 要求される手続き                                                                         |
| ---- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 軽量 | プロジェクト局所で可逆な変更(config の追記、手順の文言調整、extensions への追加) | 台帳に1行書いて、実作業でそのまま試す。比較実験は不要。効かなければ戻して結果を記す      |
| 厳格 | コアへの昇格、必須制約・権限・完了条件に触れる変更、スキルの新設                 | 第10節の比較(現行/候補/可能なら部品なし)を経てから採用。振り返りからの自動反映は行わない |

この二層により、利用者が既に許可している通常作業を止めずに(設計方針書 10.4)、危険な変更だけに評価の関門を置く。

### 6.3 記録の寿命【確定】

- 完了した作業記録は90日で `archive/` へ移す。台帳の採用・却下済みの行は年1回まとめて末尾の「済」節へ畳む。
- 繰り返す症状は台帳の回数加算で表現し、長文を毎回書かない。
- 実行ログ・スクリーンショット等の重い証拠はコミットせず、記録には所在と要約だけ書く。

## 7. 必須制約と強制点

各制約に**強制点**を必ず記す(R3)。強制点は「製品設定/実行環境/CI/運用」の4値。**「運用」しか書けない行は必須制約ではなく運用ルールであり、表中で◇印を付けて区別する**——書いた気になって守られない事態を、表の上で見えるようにするためである。

`harness/core/policy/requirements.md`(コア/利用者のみ編集。下記 deny で保護):

```markdown
# 必須制約

このファイルの変更は通常の実装修正と区別し、利用者が行う。
強制点が「運用」のみの項目(◇)は運用ルールであり、破られる前提で監視する。

| ID    | 制約                                                                | 強制点                       | 強制の設定・確認方法                                                                 | 既知の限界                                        |
| ----- | ------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------- |
| REQ-1 | 必須検証の失敗・未実行を成功として報告しない                        | CI(無ければ運用◇+独立再検証) | CI で必須検証を実行する。CI が無い場合のみ、採用判断・マージ前に別セッションで再実行 | CI に無い検証は運用頼み。再検証を省くと無力       |
| REQ-2 | このファイルと config.md の必須検証表を、エージェントが書き換えない | 製品設定                     | Claude Code: settings.json の deny(下記)。他製品: 差分レビュー(運用◇)                | denyは任意のサブプロセス経由の書込に及ばない(V10) |
| REQ-3 | 許可されていない外部送信・権限拡大をしない                          | 製品設定/実行環境            | 各製品の権限設定・サンドボックスの有効化を確認                                       | 製品ごとに段階2で実機確認                         |
| REQ-4 | 記録・配布物に認証情報・機密・無関係な個人情報を入れない            | 運用◇                        | 共有・配布前の目視。台帳・記録のテンプレートに書かない欄を設けない                   | 運用ルール扱い                                    |

Claude Code の保護設定(プロジェクトの .claude/settings.json に追記):
"permissions": { "deny": [ "Edit(./harness/core/policy/**)" ] }
※ deny は deny→ask→allow の順で最初に評価される(V10)。段階1の対象 `okf-devkit` は
CI(ubuntu・windows × Python 3.11・3.13)を持つため、REQ-1 の強制点は当初から「CI」。
```

ホスティングのアカウント開設や共同開発体制の構築はハーネスの対象外のままとするが、**既に存在する CI・ブランチ保護を強制点として使うことは対象内**とする(R6)。CI が無い間の REQ-1 の水準は「deny による定義ファイル保護+独立セッションでの再検証」である、と明示しておく。

## 8. 製品接続

### 8.1 入口の配線【確定】

正本はルートの AGENTS.md 1枚。**ただしツールが生成するネストした入口(例: `okf init` が作る `docs/AGENTS.md`)は R8 の対象外とする【確定】**——人が入口を分割したものではなく、そのディレクトリの書式規約への案内であり、撤去してもツールの再実行で復活するため。対応表(第8.3節)の「既知の制限」に有無を記す。人が置くネストした入口は段階2の検証完了まで置かない(R8——ネスト時の意味論は Codex=連結・近い方優先、Copilot=最近傍のみ、と製品間で異なることが文書上確認済み。V3・V5)。

| 製品        | 接続方法                                                                                         | 根拠(確認日 2026-08-30)      |
| ----------- | ------------------------------------------------------------------------------------------------ | ---------------------------- |
| Codex       | AGENTS.md を直接読む。追加ファイル不要                                                           | V5[S07]。合計32KiB上限に留意 |
| Claude Code | `CLAUDE.md` に `@AGENTS.md` の取込を書く(2〜3行)。Windows のためシンボリックリンクは使わない     | V1[S10]。公式推奨の方式      |
| Copilot     | AGENTS.md をネイティブに読む(文書上)。追加ファイル不要。`copilot-instructions.md` は当面作らない | V3[S12]。実機確認は段階2     |

### 8.2 スキルの方針【確定】

- **段階1はスキルを作らない。** 手順は入口からの明示参照(基本モード)で読む。スキル化は「入口の表では発見されない・呼び出しの摩擦が実測された」ときの台帳候補とする([S02]の教訓: 雑なスキルは無い方が悪い。V12)。
- スキル化するときは Agent Skills 仕様の範囲のフィールドのみ使う(name・description・license・compatibility・metadata。V9)。Claude Code 固有フィールド(`context` 等)を共有スキルに入れない。
- 性質タグは frontmatter の `metadata` に置く: `metadata: { kind: "capability-support" | "policy" | "env-adapter" | "safety", ledger: "K-00N" }`。モデル更新時に `capability-support` を機械的に列挙して棚卸しする。
- 配置の正本は当面 `.claude/skills/` のみ。`.agents/skills`・`.github/skills` への展開は H1(重複挙動の実機確認)の結果で決める——3製品共通の探索場所は存在せず(V4・V6・V7)、Codex は同名をマージせず両方表示し(V6)、Copilot の重複時挙動は未文書化(V4)のため。

### 8.3 対応表の様式(段階2で記入)

| 製品/利用形態         | 版  | 検証日 | 入口読込 | 手順追読(カナリア) | 成果物書込 | コマンド実行 | 強制点                            | 既知の制限 |
| --------------------- | --- | ------ | -------- | ------------------ | ---------- | ------------ | --------------------------------- | ---------- |
| Claude Code/CLI       | ＿  | ＿     | ＿       | ＿/10              | ＿         | ＿           | deny・フック・サンドボックス(V10) | ＿         |
| Codex/CLI             | ＿  | ＿     | ＿       | ＿/10              | ＿         | ＿           | 実装時に公式資料を再確認          | ＿         |
| Copilot/VS Code agent | ＿  | ＿     | ＿       | ＿/10              | ＿         | ＿           | 同上。無ければ「運用」と明記(R3)  | ＿         |

「文書上の対応」と「実機で確認した対応」を列で区別すること。基本モードの成立条件は**能力の4点**で判定する——入口を読む・参照先を必要時に読む・作業記録を書ける・必須検証コマンドを実行できる。これを満たさない利用形態(例: コード補完、レビュー専用画面)は基本モードの対象外として対応表に明記する。

## 9. 配布・更新・撤去(段階3)

本節は段階3で実装する設計であり、段階1〜2では読むだけでよい。専用の配布CLI・トランザクション機構は作らず、**git のコミット単位を配布の原子性として使う**(審査記録第4節丙)。

### 9.1 形態

- コアを `harness-core` リポジトリへ抽出し、タグで版付けする。版名は日付+連番(例: `2026.09.0`)【確定】。同じ識別子から常に同じ内容が取得できることを最優先とする。
- 導入は「タグの内容を `harness/core/` へコピーし、`harness/install.json` を書き、**1コミット**で入れる」。更新・巻戻しも同様に1コミット(巻戻しは `git revert` か旧タグの再導入)。
- 入口(AGENTS.md 等)への接続行は**人が追加する**。ツールを作る場合も検査(あるべき行の有無の報告)まで(R7)。

`harness/install.json`(導入記録。最小形。ハッシュ等は必要が実測されてから追加):

```json
{
  "source": "〈harness-core リポジトリの場所〉",
  "version": "2026.09.0",
  "commit": "〈タグの指すSHA〉",
  "installed_at": "2026-09-15",
  "managed": ["harness/core/"],
  "overrides": [
    {
      "path": "harness/core/procedures/verify-report.md",
      "base_version": "2026.09.0",
      "reason": "〈上流反映待ちの修正内容〉",
      "ledger": "K-007",
      "review_at": "次回更新時"
    }
  ]
}
```

`overrides` が**宣言付き上書き**(R5)である。宣言のある管理対象ファイルの改変は正当な上書きとして扱い、宣言のない改変だけを更新時の競合とする。台帳IDを書くことで、共通側に同じ修正が入った版へ更新するとき「この上書きは不要になった可能性」を機械的に列挙できる(共通側の変更履歴にも対応する台帳IDを1行書く)。

### 9.2 更新の手順

1. 新しいタグの内容を取得し、現在の `harness/core/` との差分を確認する(導入時タグとの三者比較。git のツリー比較で足りる)。
2. 宣言のない改変が見つかったら競合として止め、**三つの出口**から選ぶ——(a) 上流を採用して改変を捨てる、(b) 改変を `overrides` に宣言して保持する、(c) 手動でマージする。
3. 差分(追加・変更・削除・上書きとの重なり)を確認してから、コピーと `install.json` の更新を**1コミット**で入れる。
4. 代表作業を1件流し、入口からの到達と必須検証を確認する。スキルの発動条件・出力形式が変わる版では、文章だけの変更でも動作を確認する。
5. 問題があれば `git revert`。`state/`・`project/` は管理対象外なので巻戻しの影響を受けない。

### 9.3 撤去の手順

1. 削除候補は「`install.json` の管理対象であり、かつ宣言なしで改変されていないもの」だけ。改変されたものと `overrides` は一覧にして利用者の判断へ回す。
2. `project/`・`state/`・台帳・入口ファイルは残す。入口の接続行の削除は人が行う。
3. 撤去後にプロジェクト本来のビルド・テストが通ることを確認して完了とする。

### 9.4 シナリオとの対応

| 状況(設計方針書・依頼の想定)                     | 本仕様での受け止め                                                                    |
| ------------------------------------------------ | ------------------------------------------------------------------------------------- |
| 管理対象を直接編集した後に更新する               | 9.2 手順2で検出し、三つの出口(採用/宣言化/マージ)へ振り分ける                         |
| 共通版とローカル拡張が同じ問題を解決して両方残る | 台帳IDの相互参照(9.1)で更新時に候補を列挙し、13.3 の重複除去を実行する                |
| 二つの製品が同じスキルを複数の場所から見つける   | 正本1箇所の原則(8.2)+H1 の実機確認まで展開しない                                      |
| フックが利用できない                             | 手順は手動呼出(基本モード)。安全系は強制点の表で「運用」に降格したことが見える(第7節) |
| 更新が途中で止まる/旧版へ戻したい                | 1コミット原則により中途状態が残らない。巻戻しは revert。状態は分離済みで無傷          |
| モデル更新後、部品なしの方が速い                 | 台帳の「能力補助」行とスキルの metadata タグを列挙し、部品なし比較(第10節)→廃止       |
| 振り返りが増え続ける/完了条件が書き換えられる    | 台帳の上限・加算規則(5.5)と、REQ-2 の deny 保護+独立再検証(第7節)                     |

## 10. 評価の最小実装

評価は厳格層の変更(6.2)にだけ適用する。専用基盤は作らず、記録は台帳と作業記録で足りる。

### 比較の条件

- 比較対象: 現行版/候補版。廃止判断のときは「対象部品なし」を加える。初回導入時のみ「ハーネスなし」も1回測って基準線を残す。
- そろえるもの: 依頼文・開始時のリポジトリ状態(同一コミットから開始)・製品とモデル・利用可能なツール。片方の回答を他方に見せない。
- 安全条件(必須検証・権限)は「なし」側でも外さない。
- ばらつきが疑わしい重要な判断だけ複数回流す。全比較の複数回実施は要求しない(H4 で実コストを測って調整)。

### 記録の様式(台帳の試行行に添える)

```markdown
| 条件 | 入力          | 製品・モデル(実ID/不明) | 完了条件適合 | 人の手戻り(回数・分) | 所要 | トークン(不明可) | 備考 |
| ---- | ------------- | ----------------------- | ------------ | -------------------- | ---- | ---------------- | ---- |
| 現行 | 〈代表作業◯〉 |                         |              |                      |      |                  |      |
| 候補 | 同上          |                         |              |                      |      |                  |      |

判断: 品質が同等なら負担の少ない方。悪化が「許容できない悪化」(第2節)に触れたら不採用。
取得できない数値は「不明」と書く。ゼロとして集計しない。AIの自己採点を品質の根拠にしない。
```

## 11. 作らないもの【確定】

以下は「まだ作らない」の規定であり、禁止ではない。**トリガーが実測されるまでは着手しない**(R2)。トリガーが起きたら台帳に候補として記録し、厳格層の手続きで導入する。

**本節は新規作成の抑制であり、導入先に既にある自動化の撤去を求めるものではない【確定】。** 既存のフック・スクリプトはそのまま使い、保守負担が疑わしいものだけを台帳の観測に入れる。

| 作らないもの                                               | いまの代替                             | 再検討のトリガー                               |
| ---------------------------------------------------------- | -------------------------------------- | ---------------------------------------------- |
| 設定の組み立て・生成レイヤー、設定スキーマと競合検証ツール | プレーンなファイルと目視               | 層が2つ以上になり、実際の競合を2回経験した     |
| 検証のラッパースクリプト                                   | コマンド手動実行+4値の転記             | 転記の誤りが観測された(H3)                     |
| スキル(SKILL.md)                                           | 入口からの明示参照                     | 発見・呼出の摩擦が台帳に記録された             |
| フック・自動実行制御・継続実行ループ                       | 手順の手動呼出                         | 同じ抜けが繰り返し観測された(台帳の回数≧3目安) |
| 入口ファイルへの自動追記                                   | 人が1〜3行を追加。ツールは存在検査まで | 導入先が増え、手作業の負担が実測された         |
| ハッシュ・互換条件付きの拡張マニフェスト                   | install.json の最小形(9.1)             | 段階3で改変検出の誤り・すり抜けが起きた        |
| 専用の配布CLI・プラグイン包装                              | コピー+1コミット                       | 導入先が3以上になり手順の負担が実測された      |
| 評価の自動実行基盤・記録データベース                       | 台帳+比較記録の様式                    | 厳格層の比較が月2回を超えて発生した            |
| 組織共通層                                                 | コアとプロジェクト層の2層              | 共有する相手(人・チーム)が実際に現れた         |
| 常時稼働の複数エージェント構成                             | 必要時の独立セッション(Codex CLI 等)   | 長時間・並列の実需要が生じた                   |

## 12. 保留事項【保留】

以下は決めずに実証へ進んでよい。それぞれ決定のトリガーを添える。

- **スキルの同時配置の最終形** — H1 の実機結果で決める(段階2)。
- **Codex・Copilot の権限・サンドボックスの扱い** — 段階2のアダプター確認時に公式資料を再確認して対応表へ記す。
- **評価の数値閾値(件数・回数・許容悪化の具体値)** — 第2節の記入と H4 の実測で決める。
- **版命名の高度化(semver 等)** — 互換性で困る事態が起きたら。日付+連番で開始する。
- ~~**CI の導入・強制点の昇格**~~ — **決着済み(第0.2版)**。段階1の対象 `okf-devkit` に CI があるため、REQ-1 の強制点は当初から「CI」。
- **組織共通層の責務分割** — 共有相手の出現まで白紙のままとする。

---

## 改訂履歴

| 版  | 日付       | 内容                                                                                                                                                                                                                               |
| --- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1 | 2026-08-30 | 初版起草(HTML)。設計方針書と審査記録 HRV-2026-0830 を反映                                                                                                                                                                          |
| 0.1 | 2026-08-31 | 本 Markdown を正本化。HTML は閲覧用スナップショットに変更(内容は同一)                                                                                                                                                              |
| 0.2 | 2026-09-01 | 段階1の対象を `okf-devkit` に確定。段階1の定義を「役割の充足確認と補完」へ変更し第4節に写像表を追加。必須検証を層別化し REQ-1 の強制点を CI へ昇格。決定の振り分け規則(第1.1節)を追加。誤記2点(コア枚数・受け入れ条件の採番)を訂正 |

依拠: HARNESS_DESIGN.md(2026年8月30日版)および審査記録 HRV-2026-0830。本仕様中の製品仕様への言及(V1〜V12)はいずれも2026年8月30日の文書上の確認であり、実機検証は段階1〜2で行う。第2節の記入をもって実証を開始し、段階1の完了時に本仕様を第0.3版へ改訂する。
