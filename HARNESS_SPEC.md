# 共通AI開発ハーネス仕様

整理番号 HSP-2026-0830 / 第0.3版（2026-09-05）

本書は、mattpocock/skills、プロジェクト固有の制御、実行可能な検証、振り返りによる改善を一つのループとして運用するための実装仕様である。

> **最終方針**
> mattpocock/skills は各対象リポジトリが上流から直接インストールし、本ハーネスは再配布しない。
> 本ハーネスが所有するのは、日本語の入口、プロジェクト固有の検証・報告・引継ぎ、振り返りゲート、改善台帳、製品差のアダプターである。
> Obsidian は同じ Markdown を人間が読むための画面として使い、Markdown + Git を正本とする。

## 1. 位置づけ

### 1.1 文書の役割

| 文書 | 役割 | 扱い |
| --- | --- | --- |
| `HARNESS_DESIGN.md` | 「なぜ」の根拠集 | 凍結。編集しない |
| `HARNESS_REVIEW.html` | 第0.2版までの審査記録 | 凍結。編集しない |
| **`HARNESS_SPEC.md`（本書）** | 「何を作るか」の正本 | 生きた文書。共通の設計判断はここだけを変更する |
| `HARNESS_SPEC.html` | 第0.2版の旧スナップショット | 凍結。現行判断には使わない |
| `HARNESS_SKILLS_OVERVIEW.html` | 人間向けの図解 | 説明資料。判断が食い違う場合は本書を正とする |
| `docs/backlog/T-NNNN-*.md` | 実装単位、順序、完了条件 | 1タスク1ファイル。`docs/CONVENTIONS.md` に従う |

同じ決定を二箇所に複製しない。

| 決めたこと | 書く場所 |
| --- | --- |
| 複数タスク・複数プロジェクトに通用する規定 | 本書 |
| 一つのタスクを完了するためだけの情報 | 該当バックログ |
| 根拠がまだ一件の改善観測 | 対象プロジェクトの `harness/ledger.md` |
| 用語の定義・不可逆な設計判断 | 対象プロジェクトの `CONTEXT.md` / ADR。ただし本ハーネス自体の規定は本書 |

### 1.2 ハーネスと呼ぶ範囲

スキル群だけでは「手順集」である。次の制御ループ全体をハーネスと呼ぶ。

```text
人間の意図・受け入れ条件
          ↓
日本語の入口・ルーティング
          ↓
上流スキル + 自前のプロジェクト契約
          ↓
実装・変更
          ↓
テスト / lint / 型 / CI / レビュー
          ↓
完了報告 ── 必要な回だけ振り返り ── 改善台帳
   ↑                                      │
   └──────── 再発と効果が確認された改善 ──┘

Obsidian ⇄ 上記と同じ Markdown + Git（別の正本を作らない）
```

### 1.3 固定する設計原則

1. **上流は直接依存にする。** mattpocock/skills は対象リポジトリごとに公式の導入経路から入れ、更新判断も対象リポジトリごとに行う。
2. **自前部分を分離する。** 上流スキルと、本ハーネス固有の契約・設定・記録を同じ所有物として扱わない。
3. **正本は Markdown + Git。** Obsidian、HTML、生成 index は表示・探索・配布のための派生物とする。
4. **入口は短く、詳細は必要時に読む。** `AGENTS.md` は文脈の詰め込み場所ではなく、参照先と利用条件を示すルーターにする。
5. **小さな仕事を重くしない。** 計画、grilling、仕様化、full retrospective は、規模・不確実性・観測された摩擦に応じて選ぶ。
6. **指示と強制を分ける。** 守らせる必要がある規則は、可能な限りテスト、lint、権限設定、CIへ置く。
7. **一度の失敗を恒久ルールにしない。** 安全上の例外を除き、観測、試行、効果確認を経て昇格する。
8. **足す時に外し方も決める。** ルール、スキル、自動化には見直し契機と撤去方法を持たせる。
9. **上流の英語を無理に翻訳しない。** 人間向けの日本語案内と自前文書を日本語にし、実行文の翻訳は必要性を実測してから行う。

### 1.4 対象

- 本リポジトリは設計・仕様・バックログの置き場であり、ハーネスの適用対象ではない。
- 段階1の適用先は `okf-devkit`。
- 段階3の二つ目の適用先は `ScheLiveApp`。
- 最初に確認する製品は Claude Code と Codex。Copilot 等は実需要が出た時にアダプターを追加する。
- Windows を含み、特権・シンボリックリンクを必須にしない。

## 2. 全体構成と作成物

### 2.1 レイヤー

| レイヤー | 主な責務 | 所有者 |
| --- | --- | --- |
| 人間の操舵 | 目的、優先順位、受け入れ条件、不可逆な判断 | 利用者・チーム |
| プロジェクト入口 | 作業分類、読む場所、ローカル制約への導線 | 対象プロジェクト |
| 上流スキル | grilling、仕様化、分割、実装、TDD、レビュー、診断などの再利用可能な能力 | mattpocock/skills |
| ハーネスコア | 日本語ルート表、検証・報告・引継ぎ、retroゲート、必須制約 | 本ハーネス |
| プロジェクトアダプター | コマンド、文書配置、例外、採用スキル、製品差 | 対象プロジェクト |
| センサー・強制点 | テスト、lint、型、CI、権限、差分レビュー | 対象プロジェクト |
| 知識・状態 | CONTEXT、ADR、OKF文書、作業記録、改善台帳 | 対象プロジェクト |
| 人間向け表示 | 検索、バックリンク、プロパティ、グラフ | Obsidian（任意のUI） |

上流スキルの手順が、利用者の指示、リポジトリの制約、権限設定、必須検証を上書きすることはない。衝突した場合はローカル契約を優先し、衝突を改善台帳へ記録する。

### 2.2 対象リポジトリの既定構成

既存ファイルが同じ役割を満たす場合は新しく作らず、`harness/project/config.md` の役割対応表から実配置を参照する。

```text
<project>/
├─ AGENTS.md                         # 短い入口。プロジェクト所有
├─ CLAUDE.md                         # 必要な場合だけ製品接続
├─ .agents/skills/                   # 上流インストーラー管理
├─ skills-lock.json                  # 上流スキルの導入状態
├─ THIRD_PARTY_NOTICES.md            # 同梱物のライセンス通知
├─ harness/
│  ├─ core/                          # 本ハーネス所有
│  │  ├─ guide.md                    # 日本語の用途・呼出関係・ルート表
│  │  ├─ procedures/
│  │  │  ├─ verify-report.md         # 4値の検証と完了報告
│  │  │  ├─ retrospective.md         # retro実行条件と出力
│  │  │  └─ handover.md              # 中断・再開
│  │  ├─ templates/worklog.md        # 報告・引継ぎを兼ねる記録
│  │  └─ policy/requirements.md      # 強制点つき必須制約
│  ├─ project/                       # 対象プロジェクト所有
│  │  ├─ config.md                   # コマンド、役割対応、例外
│  │  ├─ skill-profile.md            # 採用スキル、用途、依存、更新方針
│  │  └─ extensions/                 # 本当に必要なローカル拡張
│  ├─ state/
│  │  ├─ journal/                    # 進行中・引継ぎ対象の作業記録
│  │  └─ archive/                    # 完了後の期限付き保管
│  ├─ ledger.md                      # 改善台帳
│  └─ install.json                   # ハーネスコア側の導入記録
├─ CONTEXT.md / docs/                # ドメイン知識、ADR、仕様。既存配置を優先
└─ .obsidian/                        # 原則ローカル。正本ではない
```

### 2.3 作成物の役割と更新境界

| 作成物 | 役割 | 更新ルール |
| --- | --- | --- |
| `AGENTS.md` | 作業に応じて次に読む文書・使う流れを示す | 短いポインターに限定。詳細を複製しない |
| `harness/core/guide.md` | スキルの日本語カタログと呼出関係 | 上流更新時に名称・関係だけ確認 |
| `verify-report.md` | 必須検証を実行し、結果を4値で報告する | 共通契約。プロジェクト固有コマンドは書かない |
| `retrospective.md` | full retroを行う条件と台帳への送り方 | 毎回実行を要求しない |
| `handover.md` | 別セッション・別担当が再開できる最小状態 | 推測や全文ログを入れない |
| `worklog.md` | 目的、完了条件、判断、検証、残作業、要約 | 1作業1枚。小作業は省略可 |
| `requirements.md` | 破ってはいけない制約と強制点 | 通常実装からの書換えを保護 |
| `project/config.md` | 実コマンド、範囲、既存配置、例外 | 共通更新で上書きしない |
| `project/skill-profile.md` | 採用した上流スキルと理由、依存の閉包 | `skills-lock.json` を複製せず参照する |
| `ledger.md` | 再発する摩擦と改善試行の状態管理 | 評価中は最大10件、うち試行中は最大3件。採用済みは別枠で見直す |
| `skills-lock.json` | 上流インストーラーが管理する現在状態 | 手編集しない |
| `harness/install.json` | 自前コアの出所・版・管理範囲 | 上流スキルの内容を管理対象に含めない |
| `THIRD_PARTY_NOTICES.md` | ライセンス通知 | 同梱するコピーに必要な通知を保持 |
| Obsidian Vault | 同じファイルを人が探索する | 複製ノートを作らない |

共通ガイドのカタログは用途と呼出候補を示し、そのプロジェクトでの導入済み一覧を意味しない。実際の採用状態・配置・呼出先は `project/skill-profile.md` で確認し、導入状態の正本は上流管理の記録を参照する。未導入スキルを呼び出したり自動導入したりせず、利用できない旨と次の選択を示す。候補と採用済みを区別する。

## 3. 受け入れ条件

バックログの `accepts` は次の番号を参照する。

1. ① 対象リポジトリが mattpocock/skills を上流から直接導入している。
2. ② 初期スキルプロファイルの内部参照が、未導入スキルを指していない。
3. ③ 同名スキルが複数の場所から発見されない。
4. ④ 出所、導入状態、ライセンス通知、更新方法を確認できる。
5. ⑤ 一つの短い入口から、日本語ガイドとプロジェクト制約へ到達できる。
6. ⑥ 日本語ガイドから、主要フロー、単独スキル、呼出先を把握できる。
7. ⑦ 小・通常・大の作業が別の重さで処理される。
8. ⑧ 検証結果が成功・失敗・未実行・実行不能のいずれかで正確に残る。
9. ⑨ 中断記録から別セッションで一回再開できる。
10. ⑩ 必須制約ごとに実在する強制点、または運用のみという限界が明記される。
11. ⑪ full retrospective がトリガーのある回だけ行われる。
12. ⑫ 改善候補が台帳を経由し、試行なしに恒久ルールへ昇格しない。
13. ⑬ Obsidian がリポジトリの同じ Markdown を直接開き、別の正本を作らない。
14. ⑭ OKFを使うリポジトリではfrontmatter、index、stale検査が成立し、使わないリポジトリでもコアの流れが止まらない。
15. ⑮ 代表作業3件で、入口から完了報告までの流れが成立する。
16. ⑯ 導入前との比較で、品質を落とさず手戻りまたは探索負担が改善する。
17. ⑰ Claude Code と Codex の実利用で同じ成果物契約が成立する。
18. ⑱ `ScheLiveApp` が上流スキルを自身の直接依存として導入できる。
19. ⑲ 上流スキルと自前コアを別々に更新・巻戻し・撤去でき、プロジェクト固有領域が失われない。
20. ⑳ 導入先が3件以上かつ手作業の負担が実測された場合だけ、プラグインまたは一括導入コマンドを採用する。

## 4. 日々の開発フロー

### 4.1 作業の重さを選ぶ

| 区分 | 条件 | 既定の流れ |
| --- | --- | --- |
| 小 | 目的・変更箇所・期待結果が明確で、影響が局所的 | 適切な実装スキルまたは直接実装 → 必須検証 → 短い報告 |
| 通常 | 複数の判断、振る舞い変更、通常の不具合修正・機能追加 | 必要なら `grill-with-docs` → 実装/TDD → review → 必須検証・報告 → retroゲート |
| 大 | 複数セッション、複数チケット、設計上の不確実性が高い | grilling → 必要なら prototype/research → spec → tickets → チケット単位の実装・review → 統合検証 → retroゲート |

- 「計画を立てる」は成果物を増やすことではない。通常作業では作業記録の目的・対象外・完了条件で足りる。
- grilling は、決めるべき分岐が残っている時に使う。既に決まった内容を再質問しない。
- prototype は一つの設計疑問を実行可能な形で答える時だけ使う。
- TDD は振る舞いを固定できる境界で使い、実装詳細だけを検査するテストを増やさない。
- review は「仕様への適合」と「リポジトリ標準への適合」を分けて確認する。
- reviewの比較点は着手時にコミットSHAへ解決して記録し、対象版にはHEADと未コミット変更・未追跡の対象ファイルを含める。上流スキルがコミット済み差分だけを扱う場合は、自前手順で作業ツリー差分を補完する。利用者の既存変更と今回の変更を区別し、レビュー後に対象が変わった場合は影響部分を再確認する。

### 4.2 完了契約

作業の完了には次を満たす。

1. 依頼された結果と、事前に定めた完了条件が満たされている。
2. 触った領域に必要な検証を実行し、結果を4値で記録している。
3. 失敗、未実行、実行不能、残存リスクを成功として隠していない。
4. 通常・大・高リスクの変更は、固定した比較点からreviewされている。
5. 中断する場合は、次の一手、妨げ、前提、最新の検証状態が残っている。
6. retroゲートを判定し、トリガーがなければ追加文書を作らない。

上流スキルが commit、push、外部送信などを手順に含んでいても、その実行は利用者の依頼と対象リポジトリの権限規則に従う。

### 4.3 手動開始した課題の自律実行

自律実行は、人が開始した一つの課題を、既存の依頼・権限内で実装、検証、review、報告まで進める運用を含む。定期起動、次課題の自動選択、ホスト停止中の稼働は別の能力であり、この運用の成立条件に含めない。開始手段が遠隔でも、§4.2の完了契約と§8.2の権限境界は同じである。

#### 4.3.1 開始と実行の境界

- 開始時に課題、目的・完了条件、対象外、作業場所、開始版と既存差分、許可された操作、利用枠・再試行の上限、介入先を特定する。既存合意で決まった内容は再質問しない。
- 許可範囲内の調査・編集・検証・reviewは、工程ごとの人の再指示を待たず進める。判断不足、権限不足、解消できない失敗、利用枠不足では中断し、必要な判断と次の一手を示す。上流手順や自律実行の依頼を、範囲拡大や必須制約緩和の許可に読み替えない。
- 一つの課題の実行責任を持つ親を特定し、子へ委任した対象・実行識別子・状態・成果を追跡する。子の完了だけで課題全体を完了にせず、親が検証・review・残作業を照合する。

#### 4.3.2 状態、停止、再開

作業状態は既存worklogを正本とし、実行製品の会話・実行IDと作業差分を参照する。実行状態は未開始、実行中、介入待ち、中断、完了を区別する。実行中の工程、再試行待ちの理由・回数、中断理由も記録し、検証結果の4値（§8.1）やbacklogの進捗とは混同しない。

- 工程の区切りと通常中断時に、対象版、差分、検証、子の残作業、次の一手を保存する。会話全文や新しい状態データベースを必須にしない。
- 同一課題は既存の実行を確認してから開始・再開する。重複した依頼は既存実行へ関連づけ、停止を確認できない旧実行と同じ変更先で並走しない。作業コピーの分離とアクセス権による強制を区別する。
- 停止要求を受けたら新たな変更・子の起動を止め、実行中の子やコマンドの終了を確認する。緊急停止で記録を残せなかった場合は、再開時に現物から復元する。製品の停止操作が子まで停止するかは実証し、確認不能なら次の実行を開始しない。
- 再開はworklog、現物のHEAD・未コミット・未追跡差分、依頼・設定・権限、証拠の存在、旧実行の終了を照合してから行う。副作用の成否が不明な操作はそのまま再送せず、確認または人の判断へ進む。停止や再開で試行回数・利用量をゼロに戻さない。

#### 4.3.3 再試行、時間、通知

- 一時的な通信障害への再試行と、コード修正後の再検証を分けて記録する。無制限の再試行を行わず、具体的な上限・待機方法は対象の運用設定で定める。認証失効、権限不足、対象版の不一致は同じ操作の反復で解消しない。
- 課題全体の所要、工程の時間、停滞、停止応答、親・子の合計利用量を分けて評価する。一律の短時間終了を完了条件にせず、無出力だけで停滞と断定しない。時間・予算の上限が運用上の目安か機械的な強制かを明記する。
- 完了・介入待ち・中断を、利用者が離席中にも確認できる経路へ届ける。標準通知が要件を満たせば外部通知基盤を追加しない。通知の発生と端末への到達、ターン終了と課題完了を区別して検証する。通知失敗を理由にコード変更を再実行しない。
- ホスト・アプリ・通信の停止中に実行や通知を保証できるかを明示する。無稼働中の監視や自動復旧を設けない運用では、復帰後の手動再開と最後の保存点以降の未確認範囲を扱う。

## 5. mattpocock/skills の扱い

### 5.1 導入方式

Codex等では、各対象リポジトリのルートで上流の案内どおり次を使う。

```powershell
npx skills@latest add mattpocock/skills
```

更新は対象リポジトリごとに明示的に行う。

```powershell
npx skills update
```

この方式はファイルを対象リポジトリへコピーする。したがって「直接依存」とは、実行時に上流を読むという意味ではなく、**出所と更新経路を上流へ直接つなぎ、本ハーネスの配布物へ混ぜない**という意味である。更新は自動ではない。

Claude Codeの管理プラグインを使う場合は、同じスキルを `skills` インストーラーでも重ねて入れない。

### 5.2 初期プロファイル

全スキルを無条件に入れない。`ask-matt` のルートが壊れないよう、依存関係が閉じた単位で選ぶ。

| プロファイル | 内容 | 方針 |
| --- | --- | --- |
| engineering-flow | `ask-matt`、`setup-matt-pocock-skills`、`grill-with-docs`、`grill-me`、`grilling`、`domain-modeling`、`codebase-design`、`handoff`、`prototype`、`research`、`to-spec`、`to-tickets`、`implement`、`tdd`、`code-review`、`diagnosing-bugs`、`improve-codebase-architecture`、`resolving-merge-conflicts`、`triage`、`wayfinder`、`wizard`、`to-questionnaire`、`wait-what`、`teach`、`writing-for-agents` | 段階1の候補。T-0002で参照閉包と文脈負担を実測して確定 |
| project-option | `setup-pre-commit`、Gitガードレール、移行・教材系など | 対象プロジェクトの必要がある時だけ追加 |
| laboratory | 上流の `in-progress` 配下 | 既定では導入しない。隔離した試行のみ |

この設計リポジトリに現在ある全37スキルは比較・調査用であり、対象リポジトリの既定プロファイルではない。

スキル数が増えるほど最初に読み込まれる名前・descriptionの負担が増え、一部が短縮・省略される可能性がある。T-0002では、肯定・否定のトリガープロンプトと、`ask-matt` からの参照閉包を検査する。

### 5.3 日本語化

- 上流の `SKILL.md` は英語のまま、インストーラー管理下に置く。
- `harness/core/guide.md` に「何に使うか」「いつ使わないか」「次に何を呼ぶか」を日本語で一行ずつ書く。
- `AGENTS.md`、プロジェクト設定、検証・報告、振り返り、台帳は日本語でよい。
- 起動条件に日本語が必要だと実測された場合だけ、descriptionの日英併記か別名の自前スキルを試す。
- 上流と同名の翻訳版を並置しない。Codexは同名スキルを統合しないため、二重表示と誤選択の原因になる。
- 翻訳または改変したスキルは `harness-` など別名にし、上流版と排他的に有効化する。
- 翻訳の品質は言語の印象で決めず、同じ代表タスク、肯定トリガー、否定トリガーで比較する。

### 5.4 ライセンスと出所

mattpocock/skills は MIT License であり、利用・改変・再配布は可能だが、コピーまたは主要部分には著作権表示と許諾表示を含める必要がある。

- 既定は再配布せず、各対象リポジトリが直接導入する。
- チェックインされた上流ファイルには、`THIRD_PARTY_NOTICES.md` 等で必要な通知を保持する。
- 翻訳フォークを配布する場合は、元のライセンス全文、上流URL、基準版、変更点を同梱する。
- 法的可否と保守上の推奨を分ける。再配布可能でも、更新追従のため直接依存を優先する。

### 5.5 更新

1. 対象リポジトリの作業ツリーを確認する。
2. `npx skills update` を実行する。
3. `skills-lock.json` と上流管理ファイルの差分を確認する。
4. 削除、名称変更、description変更、呼出関係の変更を日本語ガイドへ反映する。
5. 主要ルート、肯定トリガー、否定トリガー、プロジェクト必須検証を実行する。
6. 上流更新だけを一つのコミットにまとめる。問題があればそのコミットをrevertする。

上流更新と自前コア更新を同じコミットに混ぜない。

## 6. Obsidian・OKF・Markdown

### 6.1 役割分担

| 要素 | 役割 | 正本か |
| --- | --- | --- |
| Markdown + Git | 内容、差分、履歴、レビュー、復元 | **正本** |
| OKF | frontmatter、語彙、index、stale、関連の機械的規約 | 正本を検査・索引化する仕組み |
| Obsidian | 検索、バックリンク、プロパティ表示、グラフ | 同じ正本を見るUI |
| HTML | 共有・説明用の派生表示 | 正本ではない |

### 6.2 Obsidianの運用

1. 対象リポジトリのルートをVaultとして開く。
2. リポジトリ外に同じ仕様・ADR・台帳のコピーを作らない。
3. 標準MarkdownリンクとYAML frontmatterを優先し、特定のcommunity pluginがないと読めない正本を作らない。
4. `.obsidian/` は最初はGit管理外とする。複数人で価値が確認された安定設定だけを後から明示的に共有する。
5. `workspace*.json`、キャッシュ、個人の画面状態は共有しない。
6. 認証情報、個人情報、巨大ログ、チャット全文をVaultへ蓄積しない。

Obsidianで主に見るものは、`AGENTS.md`、`CONTEXT.md`、ADR、仕様、バックログ、進行中のworklog、改善台帳である。グラフはリンク構造の補助表示であり、文書品質の指標にはしない。

### 6.3 OKFの境界

- `okf-devkit` およびOKF採用リポジトリでは、既存のfrontmatter、index生成、lint、stale判定を使う。
- ハーネスコアはOKF CLIを必須依存にしない。OKF未採用リポジトリでも、Markdown、Git、役割対応表、検証契約だけで動く。
- OKF固有の配置・メタデータは `harness/project/config.md` で接続し、共通手順へ埋め込まない。
- 生成された `index.md` やHTMLを手編集しない。

### 6.4 蓄積するもの・しないもの

| 蓄積する | 原則蓄積しない |
| --- | --- |
| 現在有効な仕様、ADR、用語、検証契約 | 毎回のチャット全文 |
| 再開に必要な進行中worklog | 成功した小作業の冗長な日報 |
| 再発回数と効果が分かる改善台帳 | 同じ症状ごとの別ノート |
| 将来の判断を変える根拠 | 一度しか使わない思考過程 |

## 7. 振り返りと改善台帳

### 7.1 retroゲート

次のいずれかが起きた時だけfull retrospectiveを行う。

- 既に伝えた要件の欠落、指示の取りこぼし、誤解、不適合をAIまたは利用者が検知した。利用者による成果物・方針の修正も含むが、その指摘を待つ必要はない。
- 同じ探索、質問、手戻りを繰り返した。
- 検証漏れ、誤った成功報告、回帰、レビュー漏れがあった。
- スキルが誤発動、未発動、重複、またはローカル制約と衝突した。
- コンテキスト、ツール、権限、環境差に大きな摩擦があった。
- 作業が高コスト、高リスクになった。またはセッション間の引継ぎで情報欠落、探索・質問・実装のやり直しが起きた。

予定された選択肢の決定、説明の依頼、通常の要望追加、計画的な複数セッションへの分担だけでは発動しない。通常のTDDのredや、あらかじめ意図した失敗演習だけも摩擦とは数えない。高コスト・高リスクは作業の影響範囲や実際の負担を根拠として示し、取得できない時間・トークンを推測で数値化しない。

ゲートはAIが作業中の観測と完了・中断時の照合で判定する。利用者からの指摘を待たず、依頼・合意した要件、対象差分、検証と再試行、レビュー指摘、引継ぎ記録を照合する。通常の対話と修正の違いは語句だけでなく、既に合意した内容との関係で判断する。ゲート判定とfull retrospectiveは別工程であり、全件の深い振り返りを要求しない。

- 観測は既存worklogへ短く残し、判定には確認範囲、該当条件と根拠、処理済みの事象・台帳IDを含める。小作業でworklogがなければ通常報告でよい。
- 新しいトリガーがあればAIがfull retrospectiveと台帳更新まで進める。同じ事象の再読やセッション再開だけで再実施・回数加算しない。未処理分だけを対象にする。
- 根拠が不足した時は、取得可能な記録を先に調べる。それでも判断できなければ確認不能の範囲と次の一手を報告し、摩擦なしと断定しない。これはゲートの判断であり、検証結果の4値を増やすものではない。
- 何も該当しない回は、判定理由を含む通常の完了報告だけで終える。追加の振り返り文書・空の台帳項目は作らない。

人による全件確認を前提にしない。ただしAIによる意味の判定は運用であり、誤検知・見逃しを機械的に防ぐ強制点ではない。レビューや利用者の指摘で検知漏れが分かった場合も、その事実を改善観測として扱う。

### 7.2 full retrospectiveの出力

振り返りは「環境をどう改善できるか」の候補を重要度順に示す。候補の分類は次のとおり。

- navigation
- automated checks
- coding standards / review
- agent instructions
- tool economy
- information access
- no-op removal

振り返りから `AGENTS.md`、スキル、必須制約へ直接反映しない。まず改善台帳へ観測または候補として入れる。

### 7.3 改善台帳

状態は次の順で進める。観測・候補からも、重複・対象外などの根拠を残して却下できる。

```text
観測 → 候補 → 試行 → 採用 → 廃止
  └────┴────┴→ 却下
```

台帳の必須列は、ID、状態、初回、回数、分類、症状、対処案、適用範囲、確認方法、期限、結果・反映先とする。

- 評価中（観測・候補・試行）は最大10件、うち同時試行は最大3件。採用済みは別枠とし、却下・廃止は履歴として保持する。上限時は根拠と参照を残して統合、優先順位付け、却下を行い、新規事象を黙って捨てない。枠を空けるためだけに採用済みを廃止扱いにしない。
- 同じ症状・適用範囲の独立した発生は同じIDへ加算する。初回を1とし、同じ事象の再読・複数報告は加算しない。各発生の根拠を参照できるようにする。
- 却下・廃止した項目の再発は同じIDの履歴へ追記し、再評価する根拠と件数枠を確認して観測へ戻す。採用済みの項目で再発した場合は採用状態のまま効果を見直し、必要なら撤去案へつなぐ。
- 通常は独立した発生が累計3回で昇格検討の目安とする。回数到達だけでは採用しない。影響が大きく比較可能なら回数未満でも期限付き試行はできる。
- 試行の前に、対象と具体的な変更、期待効果、比較・確認方法、終了期限、巻戻し手順、期限切れ時の扱いを記録する。期限のない試行や、無回答による自動採用は行わない。
- 採用には試行での効果確認、期待する効果、確認方法、見直し日、撤去条件が必要。試行を中止して変更を戻す場合は却下、採用済みの改善を撤去する場合は廃止として、実際の反映・巻戻し結果を記録する。
- モデル、製品、上流スキルの大きな更新時に棚卸しする。それ以外は四半期ごと。

### 7.3.1 自律実施と人の判断

AIは観測・候補の記録、重複統合、評価中項目の優先順位付けと根拠のある却下、既存の依頼・権限内で戻せる期限付き試行、効果検証、採用・撤去案の準備までを行う。必須検証・必須制約の緩和、権限拡大、外部送信、不可逆な操作は、改善試行という名目で許可を得たことにしない。上流管理物の変更も既存の導入・変更契約に従う。

人が判断するのは恒久採用と採用済み改善の撤去、および既存の権限を越える操作である。採否を求める前に、AIが次を台帳の該当IDにまとめ、判断が必要なものだけを重要度順に提示する。

- 対象ファイル・設定と具体的な差分、変更の目的。
- 変更前後の動作の具体例、期待効果と試行で実際に確認した効果。
- 他の作業への影響、リスク、未確認の範囲。
- 検証の対象版と証拠、見直し日、撤去条件、実行可能な戻し方。

承認は対象版・差分に結びつけて記録する。承認後に変更内容が変わった場合は既存承認で代用しない。回答待ちの間は無関係の作業を進め、試行は事前に決めた期限切れ時の扱いに従う。巻戻しは当該試行の変更だけを対象にし、他の変更を巻き込む場合は停止して判断を求める。

安全・機密・不可逆な破壊の欠陥は、再発や件数枠、通常の試行・採用を待たず、既存の権限内で停止・封じ込め・強制点の修正を行える。理由、実施内容、検証、残存リスクを台帳へ記録する。この例外も権限拡大や必須制約の緩和を許可しない。

期限・見直し日の確認は作業開始・再開時と台帳更新時に行う。常時監視や定時起動が未設置なら、無稼働中に期限当日の処理が実行されるとは保証しない。期限超過した試行を次の作業へ無条件に持ち越さず、開始時に期限切れ時の扱いを実施する。

### 7.4 記録の寿命

- 進行中・引継ぎ対象だけを `state/journal/` に置く。
- 完了したworklogは既定90日で `archive/` へ移し、その後は再利用価値があるものだけ残す。
- 実行ログやスクリーンショットは必要な所在と要約だけを記録し、巨大な証拠を常時コミットしない。
- 恒久知識になった判断はCONTEXT、ADR、仕様へ移し、worklogに残したまま正本化しない。

## 8. 検証・報告・強制

### 8.1 検証結果

すべての必須検証は次の4値だけを使う。

| 値 | 意味 |
| --- | --- |
| 成功 | 対象版に対して実行し、期待条件を満たした |
| 失敗 | 実行したが期待条件を満たさなかった |
| 未実行 | 実行していない。理由を添える |
| 実行不能 | 環境・権限等で実行できない。代替確認と限界を添える |

「未実行」「実行不能」を成功へ読み替えない。修正後に再実行した場合は、失敗した事実と最終結果の両方が分かるようにする。

### 8.2 強制点

`harness/core/policy/requirements.md` の各行は、制約、強制点、確認方法、既知の限界を持つ。

強制点は、製品設定、実行環境、CI、運用のいずれかとする。運用しかない規則は「必須」と同じ強さで表現せず、破られる可能性を明記する。

最低限の制約:

1. 検証の失敗・未実行を成功として報告しない。
2. 必須検証や必須制約を、通常の実装都合で弱めない。
3. 許可されていない外部送信、権限拡大、不可逆な操作を行わない。
4. 記録・配布物へ認証情報、機密、無関係な個人情報を入れない。
5. 上流管理ファイルを無宣言で改変しない。

### 8.3 スキルの検証

上流導入・更新・翻訳試行では、最低限次を確認する。

- 肯定トリガー: 使うべき依頼で対象スキルが選ばれる。
- 否定トリガー: 似ているが対象外の依頼で誤起動しない。
- ルート: `ask-matt` や自前ガイドが存在しないスキルを案内しない。
- 成果物: 指定した場所・形式へ出力される。
- 制約: commit等を含む上流手順がローカル契約を越えない。

## 9. 配布・更新・撤去

### 9.1 二つの依存を分ける

| 対象 | 導入元 | 導入先での記録 | 更新 |
| --- | --- | --- | --- |
| mattpocock/skills | 上流リポジトリ | `skills-lock.json` + `skill-profile.md` + ライセンス通知 | 対象リポジトリが直接更新 |
| 本ハーネスのコア | 本ハーネスの配布元。将来はプラグイン | `harness/install.json` | 本ハーネスの版として更新 |
| プロジェクト固有部分 | 対象リポジトリ | Git履歴 | 共通更新では触らない |

本ハーネスのパッケージへ上流スキル本体を混ぜない。これにより、上流更新と自前更新の差分、責任、ロールバックを分離する。

### 9.1.1 共通コアの配布元を確立する

段階1では `okf-devkit` 上で実装し、段階2の実証後、二つ目のリポジトリへ導入する前に、再利用する共通コアとテンプレートを本リポジトリへ抽出する。本リポジトリを共通コアの配布用正本とし、以後の共通変更はここへ還元して版を更新する。本リポジトリ自身へのハーネス適用とは別の作業である。

最小配布物はGitで版を特定でき、導入・更新・巻戻し・撤去の明示手順を持つものとする。導入先の `harness/install.json` に配布元・版・管理範囲を記録し、`okf-devkit` もこの配布物と照合する。プロジェクト固有設定、作業記録、台帳の実データ、上流スキル本体は共通コアへ取り込まない。`ScheLiveApp` には同じ配布物から導入して移植性を検証する。

この最小配布物の確立は必須工程であり、§9.3のプラグイン化・導入自動化とは分ける。導入先3件以上という条件を待たない。

### 9.1.2 最小配布物と所有境界

- 配布物は共通コア、初回作成用のプロジェクト設定・採用スキル・空の改善台帳・入口接続の雛形、および配布対象一覧とする。作業記録の雛形はコアに含める。
- コアにプロジェクト固有の検証コマンド・CI設定・強制点の実装情報を混ぜない。共通の必須制約と、導入先が保持する具体的な強制点・限界を分離して参照で結ぶ。
- 雛形から作った設定・台帳・入口は導入後にプロジェクト所有となり、共通コア更新の管理対象に含めない。上流スキル本体や対象専用の検査スクリプトを配布しない。
- 配布版は本リポジトリの完全なGitコミットSHAで識別する。タグや専用インストーラーを最小配布の前提にしない。導入先の `install.json` に出所・SHA・管理対象を記録する。
- 正式な配布元はGitHub上の本ハーネスリポジトリとし、導入記録の `source.repository` にそのURLを記録する。配布版の確定時に、そのURLから独立したcloneを作って対象SHA・manifest・配布ファイルを取得・照合できることを確認する。出所と固定版を確認済みのローカルcloneは操作に使えるが、個人PCのパスだけを正式な配布元にしない。
- 配布元確立時には `okf-devkit` も配布物に一致するコアと導入記録へ移行し、固有情報・既存の検証機能・作業記録・台帳の保持を確認する。

日常の手順は配布コアとプロジェクト設定から読めるようにし、導入先に本仕様書のコピーを要求しない。仕様の根拠は導入記録が示す配布元・固定コミットの本仕様書として参照する。配布コアに導入ごとのSHAや個人環境の絶対パスを埋め込まない。

### 9.1.3 配布対象一覧と導入記録

最小配布の記録形式はバージョン1とし、未知の形式を推測して操作しない。

| 記録 | 必須項目 |
| --- | --- |
| 配布元の `distribution/manifest.json` | `schema_version: 1`、`hash_format: "sha256-canonical-text-v1"`、`files`。各要素は `source`（配布元ルート相対パス）、`target`（導入先ルート相対パス）、`mode` |
| 導入先の `harness/install.json` | `schema_version: 1`、`hash_format`、`source`（`repository`・完全SHAの `commit`・`manifest` パス）、`manifest_sha256`、`installed_at`（UTC時刻）、`managed_files`。各管理要素は `source`・`target`・`sha256` |

`mode` は `core`（継続管理）、`seed`（初回作成用）、`manual`（入口等へ人が確認して反映する断片）の3値とする。`core` の配置先は `harness/core/` 配下に限定する。`seed` と `manual` は導入後の管理ハッシュ一覧に入れない。`install.json` 自身は導入記録として別扱いにし、自己ハッシュの対象にしない。

配布物コミットを先に確定し、そのコミット内のmanifestとファイルから導入記録を生成する。manifestには自分を含むコミットのSHAや自分自身のハッシュを書かない。配布版はブランチ名・最新HEAD・未コミットのコピー元で代用しない。出所のコミットとmanifestを解決できない場合、導入・更新・巻戻し・撤去を開始しない。

ハッシュ対象はUTF-8のテキストとし、BOM付き・不正UTF-8は不適合として止める。CRLFおよび単独CRをLFへ正規化してUTF-8に再符号化したバイト列のSHA-256を小文字16進数で記録する。末尾改行の有無、空白、文字の正規化は変更しない。この照合は改行差だけを無視する内容一致であり、Gitのblob IDや生バイト一致とは区別する。manifestにも同じハッシュ方式を用いる。

ファイル一覧は明示列挙とし、絶対パス・`..`・glob・重複・大文字小文字だけが異なる配置先・対象ルート外への解決を許さない。配布・導入対象とその親経路にシンボリックリンクやjunctionがある場合は、この最小手順では操作せず止める。`install.json` の管理一覧とハッシュは、記録された配布元コミットのmanifest・コアから再計算して照合し、記録の自己申告だけを根拠に削除・上書きしない。

### 9.2 段階1〜3の導入

1. 対象リポジトリで上流スキルを直接導入する。
2. 選択したプロファイルと参照閉包を確認する。
3. 本ハーネスのコアとプロジェクトアダプターを設置する。
4. 入口へ必要な参照を人が確認して追加する。
5. 上流と自前を別コミットにし、代表作業で検証する。

更新、巻戻し、撤去は依存ごとに別々に行う。`project/`、`state/`、`ledger.md`、既存文書、入口のプロジェクト固有部分を自動削除しない。

撤去前に入口とプロジェクト文書のコア参照を調べ、該当箇所だけを確認して手動編集する。自動除去用マーカーで入口を管理しない。参照解消を確認してからコアを削除し、プロジェクト本来の指示・検証への導線が残ることを確認する。

最小配布の手順は、一時的な検証用Gitリポジトリで導入・更新・巻戻し・撤去を実行して確認する。固有領域の保持、再導入で重複しないこと、管理対象への手修正を上書きする前に停止できることを検証する。実プロジェクトでの移植性検証とは区別する。

### 9.2.1 操作前の照合と中断

新規導入、導入記録のない既存pilotの移行、導入済みコアの更新は別の手順にする。新規導入では既存コアを取り込まない。pilot移行では比較点・既存ファイル・意図した移設差分を事前に列挙して照合する。固有領域の保持は、その列挙した移設差分だけを許容し、既存機能と実データが保持された状態を指す。

通常の更新・巻戻し・撤去は、操作前に現行の導入記録と全管理ファイルを照合する。手修正、欠落、不明な既存ファイル、記録不整合、競合があれば、どのファイルも変更する前に止める。自動マージや強制上書きはしない。変更予定の配置先と管理ディレクトリ内の未知ファイルも確認し、削除対象は記録と配布元の両方で裏づけられるファイルだけにする。

更新・巻戻しは現行版と目的版の一覧の和集合で追加・変更・削除を確認する。目的版で不要になった現行管理ファイルは、現行ハッシュとの一致を確認したものだけ削除する。雛形を再適用せず、入口・プロジェクト設定が目的版の手順や参照条件を満たすかを確認する。満たさない場合は必要なプロジェクト変更を別途明示し、コア操作に紛れて上書きしない。

操作対象と操作前の内容・不存在、現在版・目的版、復旧方法を作業記録と退避先に残す。変更中は対象への並行編集を避ける。中断・途中失敗を完了として記録せず、再開時は変更済み範囲を照合し、当該操作の変更だけを戻してから再試行する。操作後に加わった別の変更と競合する場合は止めて判断を求める。リポジトリ全体のresetや広いrevertで巻き戻さない。

導入記録はコア反映・参照確認・必要検証が揃った後に確定し、コアと対応する変更としてGitに残す。記録を書き終えた時点だけで成功にせず、最終的な配布元・管理対象・内容の再照合を行う。

### 9.3 将来のプラグインと一括導入

複数リポジトリ・複数利用者へ本ハーネス固有のスキルを配る段階では、Codex向けにはプラグインを優先する。プラグインへ入れるのは本ハーネス所有のスキル、テンプレート、必要な接続情報だけである。

一つの導入コマンドを作る場合も、その役割はオーケストレーションに限定する。

1. 本ハーネスのプラグインまたはコアを導入・更新する。
2. 対象リポジトリ上で mattpocock/skills の公式インストーラーを呼ぶ。
3. プロファイル、重複、ライセンス通知、入口、必須検証を検査する。
4. それぞれの失敗を別々に表示し、上流を自前パッケージへコピーしない。

着手トリガーは、導入先が3リポジトリ以上であり、手作業の反復・誤り・更新負担が実測されていることとする。

## 10. 段階計画と評価

### 段階0 設計基準の更新（本リポジトリ）

- 本書を第0.3版へ更新する。
- 未着手バックログを本方針へ全面改訂する。
- `HARNESS_SKILLS_OVERVIEW.html` を本方針に合わせる。
- 本リポジトリの全37スキルは調査用であり、適用完了とは数えない。

### 段階1 okf-devkitへの最小導入

- T-0002〜T-0007を順に進める。
- 上流直接依存、日本語ガイド、検証・報告・引継ぎ、強制点、retroゲート、台帳、Obsidian/OKF接続を設置する。
- 着手直前の一件だけ詳細化する。

### 段階2 実作業と製品間の確認

- T-0008で代表作業3件を行う。
- 少なくともClaude CodeとCodexで、同じ成果物契約が成立するか実測する。
- 導入前または部品なしの基準と比較し、品質を落とさず手戻りを減らすか確認する。

### 段階3 二つ目のリポジトリ

- T-0013で共通コアの配布元を確立する（→ §9.1.1）。原則T-0008完了後、T-0009着手前に行う。今回の実装開始条件と製品実証の未確認範囲はT-0013の「決定と根拠」に記録した利用者判断に従う。実証していない受け入れ条件の達成を意味しない。
- T-0009で `ScheLiveApp` へ導入する。
- 上流スキルはScheLiveApp自身が直接導入する。
- 上流、自前コア、プロジェクト固有部分の更新・巻戻し・撤去を演習する。

### 段階4 配布の製品化

- T-0010はトリガーが満たされた時だけ着手する。
- 本ハーネス固有部分をプラグイン化し、必要なら一括導入コマンドを追加する。

### 10.1 最小評価

| 観測 | 記録 |
| --- | --- |
| 完了条件への適合 | 適合 / 不適合と理由 |
| 人の修正 | 回数と概算時間 |
| 探索・質問の反復 | 回数 |
| 検証の誤報・漏れ | 件数 |
| スキルの未発動・誤発動・重複 | 件数 |
| 所要・トークン | 取得可能な場合だけ。取得不能を0にしない |
| 文脈負担 | スキル一覧の省略警告、入口の長さ、不要な読込 |

比較時は、同じ依頼、同じ開始コミット、同じ製品・モデル、同じ利用可能ツールをそろえる。AIの自己採点だけを品質の根拠にしない。

## 11. まだ作らないもの

| 作らないもの | いまの代替 | 再検討のトリガー |
| --- | --- | --- |
| 上流スキル全部の日本語フォーク | 原文 + 日本語ガイド | 同じ理解・起動問題が複数回再発し、比較で翻訳版が改善 |
| 上流スキルの自前パッケージへの同梱 | 各リポジトリから直接導入 | オフライン等の実要件。実施時もライセンス・追従方式を別設計 |
| 全リポジトリ一律のスキル全部入り | 参照閉包を満たすプロファイル | 実利用データで不足が確認された |
| 毎回のfull retrospective | retroゲート | 見逃しが繰り返され、常時実行の方が低コスト |
| チャット全文・巨大ログの知識庫化 | worklog要約 + 台帳 | 具体的な検索需要と保持期間が定義された |
| Obsidian community plugin必須の正本 | 標準Markdownとfrontmatter | 複数人で便益と移行方法を検証済み |
| ハーネス専用データベース | Markdown + Git + OKF | 検索・集計不能が反復し、ファイルでは解けない |
| 専用配布CLI・一括導入 | 明示手順 + 別コミット | 3リポジトリ以上で負担を実測 |
| 常時自動更新 | 明示更新 + diff + 検証 | 互換性、通知、ロールバックを実証済み |
| 常時稼働の複数エージェント | 必要時の分離レビュー・調査 | 長時間・並列の実需要 |

## 12. 実証で確定する項目

方針は確定しているが、次の値は実証結果で更新してよい。

- engineering-flowプロファイルの最終スキル数と、`ask-matt` を保つための最小参照閉包。
- Claude CodeとCodexで共有できる配置、呼出方法、強制点の差。
- `.obsidian/` から共有する設定が本当に必要か。
- 台帳の発生回数、評価中10件・同時試行3件の上限、採用済み項目の見直し負担、worklogの90日保管が実負担に合うか。
- プラグイン化・一括導入へ進む時点と、製品ごとの包装方法。

---

## 改訂履歴

| 版 | 日付 | 内容 |
| --- | --- | --- |
| 0.1 | 2026-08-30 | 初版 |
| 0.2 | 2026-09-01 | `okf-devkit` を段階1の対象に固定し、役割写像、強制点、配布設計を具体化 |
| 0.3 | 2026-09-05 | mattpocock/skillsを対象リポジトリの直接依存とし、自前ハーネス、言語方針、Obsidian/OKF、retroゲート、プラグイン化条件を全面再設計 |

## 参照

- mattpocock/skills README: https://github.com/mattpocock/skills/blob/main/README.md
- mattpocock/skills LICENSE: https://github.com/mattpocock/skills/blob/main/LICENSE
- OpenAI「スキルの構築」: https://learn.chatgpt.com/ja-JP/docs/build-skills
- OpenAI「プラグインを作成する」: https://learn.chatgpt.com/ja-JP/docs/build-plugins
- Obsidian Help「How Obsidian stores data」: https://obsidian.md/help/Files%2Band%2Bfolders/How%2BObsidian%2Bstores%2Bdata
- 着想元「楽をするための AI ハーネス」: https://kawasin73.hatenablog.com/entry/2026/08/22/124436
